{
  "name": "xshawalx-prompts",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "check": "npm run validate && npm run build"
  },
  "engines": { "node": ">=20" }
}
